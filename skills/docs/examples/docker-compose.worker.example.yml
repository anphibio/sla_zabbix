services:
  worker:
    build:
      context: .
      dockerfile: worker/Dockerfile
    container_name: app_worker
    env_file:
      - .env
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
